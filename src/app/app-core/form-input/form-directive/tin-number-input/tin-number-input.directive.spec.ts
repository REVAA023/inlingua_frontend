import { TinNumberInputDirective } from './tin-number-input.directive';

describe('TinNumberInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TinNumberInputDirective();
    expect(directive).toBeTruthy();
  });
});
